// Definition of the variable with the message
let message = "Hello, welcome to my first JavaScript app!";

// Assigning the message to a DOM element
document.getElementById('message').textContent = message;
